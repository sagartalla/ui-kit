{
  "name": "acko-ui-kit",
  "version": "independent",
  "devDependencies": {
    "lerna": "^3.18.3"
  },
  "scripts": {
    "tsc": "lerna run tsc",
    "publish": "lerna run tsc && lerna run publish"
  }
}
